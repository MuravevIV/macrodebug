package macrodebug

import org.junit.Test

class ArrayForeachWithIndexMacroTest {

    import macrodebug.ArrayForeachWithIndexMacro._

    @Test
    def apply(): Unit = {
        /*
        arrayForeachWithIndex(Array(1, 2, 3, 4, 5)) { (item, index) =>
            println(item, index)
        }
        */
    }
}
