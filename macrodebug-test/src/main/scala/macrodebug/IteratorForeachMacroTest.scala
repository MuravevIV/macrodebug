package macrodebug

import org.junit.Test

class IteratorForeachMacroTest {

    import macrodebug.IteratorForeachMacro._

    @Test
    def apply(): Unit = {
        /*
        iteratorForeach(Iterator(1, 2, 3, 4, 5))(println(_))
        */
    }
}
